---
cflag_groups:
  debug: [-Og, -g]
  opt: [-O2]

extra_dist: [extra]

project:
  name: yambs
  github:
    owner: &self vkottler

variants:
  clang:
    cflag_groups: [debug]

  clang-opt:
    extra_cflags: [--coverage]
    cflag_groups: [opt]

dependencies:
  - github: {repo: yambs-sample, owner: *self}
  - directory: ../native2
