---
includes:
  - toolchain_common.yaml
  - entry_common.yaml

type: object
additionalProperties: false

properties:
  enabled:
    type: boolean
    default: true

  cflag_groups:
    type: array
    default: []
    items:
      type: string

  ldflag_groups:
    type: array
    default: []
    items:
      type: string

  targets:
    type: array
    default: [apps, lib]
