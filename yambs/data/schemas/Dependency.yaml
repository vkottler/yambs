---
type: object
additionalProperties: false
properties:
  kind:
    type: string
    enum: [yambs]
    default: yambs

  source:
    type: string
    enum: [github]
    default: github

  github:
    $ref: package://yambs/schemas/Github.yaml
