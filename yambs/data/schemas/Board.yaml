---
type: object
additionalProperties: false
required: [name, chip]

properties:
  name:
    type: string
  chip:
    type: string

  extra_cflags:
    type: array
    default: []
    items:
      type: string

  targets:
    type: array
    default: [apps, dumps, hexs]
